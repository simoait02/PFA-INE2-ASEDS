apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-config
  namespace: twitch
data:
  database: videos

---
apiVersion: v1
kind: Service
metadata:
  name: mongo-video
  namespace: twitch
spec:
  clusterIP: None
  ports:
    - port: 27017
      name: mongo
  selector:
    app: mongo-video

---
apiVersion: v1
kind: Service
metadata:
  name: video-database-service
  namespace: twitch
spec:
  selector:
    app: mongo-video
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo-video
  namespace: twitch
spec:
  serviceName: "mongo-video"
  replicas: 1
  selector:
    matchLabels:
      app: mongo-video
  template:
    metadata:
      labels:
        app: mongo-video
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "video-role"
        vault.hashicorp.com/agent-inject-token: "true"
        vault.hashicorp.com/agent-inject-secret-MONGO_USER: "secret/data/video"
        vault.hashicorp.com/agent-inject-template-MONGO_USER: |
          {{- with secret "secret/data/video" -}}
          {{ .Data.data.mongo_user }}
          {{- end }}
        vault.hashicorp.com/agent-inject-secret-MONGO_PASSWORD: "secret/data/video"
        vault.hashicorp.com/agent-inject-template-MONGO_PASSWORD: |
          {{- with secret "secret/data/video" -}}
          {{ .Data.data.mongo_password }}
          {{- end }}
        vault.hashicorp.com/agent-inject-secret-MONGO_DB: "secret/data/video"
        vault.hashicorp.com/agent-inject-template-MONGO_DB: |
          {{- with secret "secret/data/video" -}}
          {{ .Data.data.mongo_db }}
          {{- end }}
    spec:
      containers:
        - name: mongo
          image: mongo
          ports:
            - containerPort: 27017
              name: mongo
          env:
            - name: MONGO_INITDB_ROOT_USERNAME_FILE
              value: "/vault/secrets/mongo_user"
            - name: MONGO_INITDB_ROOT_PASSWORD_FILE
              value: "/vault/secrets/mongo_password"
            - name: MONGO_INITDB_DATABASE_FILE
              value: "/vault/secrets/mongo_db"
          volumeMounts:
            - name: mongo-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: mongo-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
